name: animated_countdown
description: Animated Countdown Widget.
version: 0.0.3
homepage: https://github.com/tajaouart/animated_countdown

environment:
  sdk: '>=2.18.5 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
